<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="UsefulAIKey.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:UsefulAIKey"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="UsefulAIKey">

    <StackPanel Orientation="Vertical" HorizontalAlignment="Left">
        <!--<TextBlock Text="Your Copilot key will..." Margin="10"/>-->
        <ComboBox x:Name="ActionComboBox"
                  SelectionChanged="Action_SelectionChanged"
                  Header="Your Copilot key will..." Width="200">
            <ComboBox.ItemTemplate>
                <DataTemplate x:DataType="local:ActionOption">
                    <TextBlock Text="{x:Bind Label}"/>
                </DataTemplate>
            </ComboBox.ItemTemplate>
        </ComboBox>

        <Line></Line>

        <ScrollViewer Height="200" Width="400">
            <TextBox x:Name="OutputTextBox" AcceptsReturn="True" TextWrapping="Wrap" IsReadOnly="True"/>
        </ScrollViewer>
    </StackPanel>
</Window>
